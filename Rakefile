#!/usr/bin/env ruby

#require 'cucumber'
#require 'cucumber/rake/task'

#::Cucumber::Rake::Task.new(:features) do |t|
#  t.cucumber_opts = "features --format pretty"
#end

task :default => [:test]

desc "run lime specifications"
task :spec do
  sh 'rubytest'
end

desc "run lime specifications (same as spec)"
task :test do
  sh 'rubytest'
end

